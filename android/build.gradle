apply plugin: 'com.android.library'

buildscript {
    repositories {
        jcenter()
        maven { url 'https://maven.google.com' }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.1'
    }
}

android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 29
        versionCode 52
        versionName "0.5.2"
        project.archivesBaseName = "react-native-ffmpeg"
    }
    lintOptions {
        abortOnError false
    }
}

repositories {
    maven { url "$rootDir/node_modules/react-native/android" }
    mavenCentral()
    jcenter()
    maven { url 'https://maven.google.com' }
    maven { 
        url "https://jitpack.io"
        credentials {
            username = "Fausto95"
            password = "jp_hk8n979i9vndo7iunuhn258o6m"
        }
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
    implementation 'com.github.Fausto95:mobile-ffmpeg:1.0.1'
}
